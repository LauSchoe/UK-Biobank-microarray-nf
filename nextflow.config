process {
    container = 'ukb_image'
}

profiles {
    development {
        workDir                 = 'work'
        docker.enabled          = true
        conda.enabled           = true
        process.maxErrors       = 10
}
}
